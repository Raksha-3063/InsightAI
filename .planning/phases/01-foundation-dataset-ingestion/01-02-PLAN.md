---
phase: 01-foundation-dataset-ingestion
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/models/project.py
  - backend/app/schemas/project.py
  - backend/app/routes/project.py
  - backend/app/models/dataset.py
  - backend/app/schemas/dataset.py
  - backend/app/routes/dataset.py
  - backend/app/services/parser.py
  - backend/app/main.py
  - backend/tests/test_projects.py
  - backend/tests/test_dataset.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - User can create a project using POST /api/projects which returns 201 Created and lists it under GET /api/projects.
    - User can upload CSV and Excel (.xlsx) files to a project, which are saved in local storage.
    - System calculates and saves dataset stats (row/col count, duplicates, missing values, memory usage, types) in MongoDB.
  artifacts:
    - backend/app/models/project.py
    - backend/app/routes/project.py
    - backend/app/services/parser.py
    - backend/app/routes/dataset.py
  key_links:
    - backend/app/routes/project.py and backend/app/routes/dataset.py are imported and registered in backend/app/main.py
    - backend/app/routes/dataset.py uses backend/app/services/parser.py
---

<objective>
Implement database models and APIs for Project Management and Dataset Uploads, including automatic parsing of CSV and Excel metadata using Pandas.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-dataset-ingestion/01-CONTEXT.md
@backend/app/database/connection.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Management Models and API Routes</name>
  <files>backend/app/models/project.py,backend/app/schemas/project.py,backend/app/routes/project.py,backend/app/main.py</files>
  <read_first>backend/app/database/connection.py</read_first>
  <action>
    Create Project DB model in backend/app/models/project.py containing projectName, description, userId, and createdAt.
    Create Project Pydantic schemas in backend/app/schemas/project.py.
    Create backend/app/routes/project.py implementing POST /api/projects (create project) and GET /api/projects (list projects for user). Include route GET /api/projects/{projectId} for project details.
    Include current user JWT dependency to secure all project routes.
    Register project router in backend/app/main.py.
  </action>
  <verify>Call POST /api/projects with a JWT token and verify successful creation</verify>
  <acceptance_criteria>
    - POST /api/projects yields 201 Created and saves project referencing current user
    - GET /api/projects returns a list of projects created by that user
  </acceptance_criteria>
  <done>Project isolation database schema and CRUD APIs are implemented</done>
</task>

<task type="auto">
  <name>Task 2: Dataset Database Model and File Uploader Helpers</name>
  <files>backend/app/models/dataset.py,backend/app/schemas/dataset.py</files>
  <read_first>backend/app/models/project.py</read_first>
  <action>
    Create Dataset DB model in backend/app/models/dataset.py with columns: projectId, fileName, filePath, rows, columns, missingValues, duplicateRows, size, uploadDate.
    Create Dataset Pydantic validation schemas in backend/app/schemas/dataset.py.
    Set up uploads folder path backend/app/uploads/ dynamically via configuration.
  </action>
  <verify>Ensure backend/app/uploads directory is created and accessible</verify>
  <acceptance_criteria>
    - backend/app/models/dataset.py includes projectId, fileName, rows, columns, missingValues, duplicateRows
  </acceptance_criteria>
  <done>Dataset database structures and upload folder paths are defined</done>
</task>

<task type="auto">
  <name>Task 3: Tabular Metadata Parser Service</name>
  <files>backend/app/services/parser.py</files>
  <read_first>backend/requirements.txt</read_first>
  <action>
    Create backend/app/services/parser.py containing:
      - Function parse_file_metadata(file_path, file_extension) using Pandas and openpyxl.
      - Calculate: total rows, total columns, missing values count (isnull().sum().sum()), duplicate rows count (duplicated().sum()), file size, and memory usage.
      - Detect column types: map columns to "numerical", "categorical", or "datetime".
  </action>
  <verify>Run a python script calling parse_file_metadata on a sample CSV and xlsx file</verify>
  <acceptance_criteria>
    - parse_file_metadata correctly identifies columns, rows, missing, duplicates
    - Supports both CSV and Excel (.xlsx) extensions
  </acceptance_criteria>
  <done>Pandas-based metadata extractor service is ready and tested on dummy datasets</done>
</task>

<task type="auto">
  <name>Task 4: Dataset Upload API Routes</name>
  <files>backend/app/routes/dataset.py,backend/app/main.py</files>
  <read_first>backend/app/services/parser.py,backend/app/models/dataset.py</read_first>
  <action>
    Create backend/app/routes/dataset.py implementing POST /api/projects/{projectId}/datasets/upload accepting raw files via UploadFile.
    Validate file extension (.csv, .xlsx). Save the file to local disk (backend/app/uploads/).
    Call parse_file_metadata on the saved file using run_in_threadpool.
    Save metadata schema in MongoDB under dataset collection and return a 201 Created response.
    Register dataset router in backend/app/main.py.
  </action>
  <verify>Upload a CSV file via API client and assert returned stats matches file contents</verify>
  <acceptance_criteria>
    - POST /api/projects/{projectId}/datasets/upload parses CSV and Excel files
    - Returns JSON structure containing rows, columns, missingValues, duplicateRows, and schema
    - Saves metadata record to dataset collection referencing the projectId
  </acceptance_criteria>
  <done>Dataset upload API router is integrated and successfully parses uploads</done>
</task>

<task type="auto">
  <name>Task 5: Project and Upload Integration Testing</name>
  <files>backend/tests/test_projects.py,backend/tests/test_dataset.py</files>
  <read_first>backend/app/routes/project.py,backend/app/routes/dataset.py</read_first>
  <action>
    Create backend/tests/test_projects.py asserting project creation and listing security bounds.
    Create backend/tests/test_dataset.py using mock CSV and Excel uploads to verify metadata calculations.
  </action>
  <verify>Run pytest backend/tests/</verify>
  <acceptance_criteria>
    - pytest succeeds and all tests pass
  </acceptance_criteria>
  <done>Project management and dataset parsing integration tests pass green</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run all tests: `pytest backend/tests/`
- Start server and perform test upload of CSV/Excel via swagger docs.
</verification>

<success_criteria>
- Project creation isolation works.
- Uploaded files are successfully stored and parsed.
- Numeric and Excel type casting is automatically logged in MongoDB.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dataset-ingestion/01-02-SUMMARY.md`
</output>
