---
phase: 01-foundation-dataset-ingestion
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/database/connection.py
  - backend/app/models/user.py
  - backend/app/schemas/user.py
  - backend/app/auth/service.py
  - backend/app/auth/routes.py
  - backend/tests/conftest.py
  - backend/tests/test_auth.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - User can register an account by sending POST /api/auth/register which returns a 201 status code and the created user schema without password.
    - User can log in by sending POST /api/auth/login which returns a 200 status code and a valid JWT bearer token.
    - Decoded JWT tokens map back to valid user records in the MongoDB database.
  artifacts:
    - backend/requirements.txt
    - backend/app/main.py
    - backend/app/database/connection.py
    - backend/app/auth/service.py
    - backend/app/auth/routes.py
  key_links:
    - backend/app/main.py imports and includes backend/app/auth/routes.py router
    - backend/app/auth/routes.py uses backend/app/auth/service.py and backend/app/database/connection.py
---

<objective>
Initialize the FastAPI backend project structure, set up async MongoDB Atlas database connection, implement user models and schemas, and establish JWT-based authentication.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-dataset-ingestion/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Project Setup and Configuration</name>
  <files>backend/requirements.txt,backend/app/config.py,backend/app/main.py</files>
  <read_first>.planning/phases/01-foundation-dataset-ingestion/01-CONTEXT.md</read_first>
  <action>
    Create backend/requirements.txt with fastapi, uvicorn, motor, pydantic[email], PyJWT, passlib[bcrypt], python-multipart, pytest, pytest-asyncio.
    Create backend/app/config.py using Pydantic Settings to load MONGODB_URL, DATABASE_NAME (default: "insightai"), JWT_SECRET, and ACCESS_TOKEN_EXPIRE_MINUTES (default: 60) from environment.
    Initialize backend/app/main.py with standard FastAPI setup, CORS middleware, startup/shutdown database hooks, and healthcheck endpoint.
  </action>
  <verify>Run uvicorn backend.app.main:app --reload and query http://localhost:8000/health</verify>
  <acceptance_criteria>
    - backend/requirements.txt lists fastapi, motor, PyJWT, passlib, uvicorn
    - backend/app/config.py contains Settings class inheriting from BaseSettings
    - GET /health returns {"status": "ok"}
  </acceptance_criteria>
  <done>FastAPI backend runs and environment variables load successfully</done>
</task>

<task type="auto">
  <name>Task 2: MongoDB Connection and User Database Models</name>
  <files>backend/app/database/connection.py,backend/app/models/user.py,backend/app/schemas/user.py</files>
  <read_first>backend/app/config.py</read_first>
  <action>
    Implement backend/app/database/connection.py using motor.motor_asyncio.AsyncIOMotorClient to connect to MongoDB and expose database helper reference.
    Define User DB model in backend/app/models/user.py with fields: name, email, hashed_password, createdAt.
    Define Pydantic schemas in backend/app/schemas/user.py: UserCreate (validate email and password), UserResponse (exclude password).
  </action>
  <verify>Run a script to write and read a mock user using database connection</verify>
  <acceptance_criteria>
    - backend/app/database/connection.py implements db client helper
    - backend/app/schemas/user.py has UserResponse schema excluding password
  </acceptance_criteria>
  <done>MongoDB Atlas database client is ready and user data validation schemas are set up</done>
</task>

<task type="auto">
  <name>Task 3: Passlib and JWT Cryptographic Authentication Services</name>
  <files>backend/app/auth/service.py</files>
  <read_first>backend/app/config.py</read_first>
  <action>
    Create backend/app/auth/service.py containing:
      - CryptContext helper for password hashing using bcrypt.
      - Functions verify_password(plain, hashed) and get_password_hash(plain).
      - Function create_access_token(data) using PyJWT.
      - Function decode_access_token(token) which validates signature, expiration, and extracts the subject (email).
  </action>
  <verify>Run python tests to assert password hashing and JWT token roundtrips</verify>
  <acceptance_criteria>
    - backend/app/auth/service.py contains get_password_hash and verify_password
    - create_access_token signs tokens using hs256 and config secret key
  </acceptance_criteria>
  <done>JWT authentication and password hashing utilities are fully functional</done>
</task>

<task type="auto">
  <name>Task 4: Authentication API Routes</name>
  <files>backend/app/auth/routes.py,backend/app/main.py</files>
  <read_first>backend/app/auth/service.py,backend/app/schemas/user.py</read_first>
  <action>
    Create backend/app/auth/routes.py implementing registration POST /api/auth/register and login POST /api/auth/login.
    Implement GET /api/auth/profile requiring OAuth2PasswordBearer Bearer token dependency to fetch current user data.
    Register auth router in backend/app/main.py under prefix "/api/auth".
  </action>
  <verify>Send register and login requests using curl or http client</verify>
  <acceptance_criteria>
    - POST /api/auth/register yields 201 Created and saves user with hashed password
    - POST /api/auth/login yields access_token and token_type "bearer"
    - GET /api/auth/profile returns user details with valid token
  </acceptance_criteria>
  <done>User authentication API endpoints are implemented and exposed</done>
</task>

<task type="auto">
  <name>Task 5: Auth Module Unit and Integration Testing</name>
  <files>backend/tests/conftest.py,backend/tests/test_auth.py</files>
  <read_first>backend/app/auth/routes.py</read_first>
  <action>
    Create backend/tests/conftest.py defining a test database client fixture.
    Create backend/tests/test_auth.py writing test cases for register, login, profile endpoints, and invalid token denials.
  </action>
  <verify>Run pytest backend/tests/test_auth.py</verify>
  <acceptance_criteria>
    - pytest runs successfully and test_auth.py tests pass
    - Database is mocked or isolated during test run
  </acceptance_criteria>
  <done>Backend authentication tests are in place and verified green</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run all tests: `pytest backend/tests/`
- Build and verify ASGI startup: `uvicorn backend.app.main:app --reload`
</verification>

<success_criteria>
- All auth unit tests pass.
- API endpoints handle registrations, login, and token-restricted requests.
- MongoDB collections for users are automatically created and searchable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dataset-ingestion/01-01-SUMMARY.md`
</output>
