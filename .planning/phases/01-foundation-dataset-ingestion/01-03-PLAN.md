---
phase: 01-foundation-dataset-ingestion
plan: 01-03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/package.json
  - frontend/src/services/api.ts
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/login/page.tsx
  - frontend/src/app/register/page.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/ProjectCard.tsx
  - frontend/src/components/FileUploader.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - User can navigate register/login pages, enter details, and login to redirect to the dashboard.
    - Dashboard lists existing projects and shows a "Create Project" button.
    - User can select a project and upload a CSV/Excel file through the file uploader component, showing a progress spinner and metadata cards upon completion.
  artifacts:
    - frontend/src/contexts/AuthContext.tsx
    - frontend/src/services/api.ts
    - frontend/src/app/dashboard/page.tsx
    - frontend/src/components/FileUploader.tsx
  key_links:
    - frontend/src/app/layout.tsx wraps children in AuthProvider from frontend/src/contexts/AuthContext.tsx
    - frontend/src/components/FileUploader.tsx uses axios client from frontend/src/services/api.ts to upload files to backend
---

<objective>
Scaffold the Next.js React frontend project, implement state-management context for JWT authentication, and build the premium dashboard pages for project and dataset management.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-dataset-ingestion/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js Frontend App Scaffolding</name>
  <files>frontend/package.json</files>
  <read_first>.planning/phases/01-foundation-dataset-ingestion/01-CONTEXT.md</read_first>
  <action>
    Initialize Next.js app in the folder "frontend" using:
    `npx -y create-next-app@latest frontend --ts --tailwind --app --src-dir --eslint --import-alias "@/*" --use-npm`
    Install axios, lucide-react, and canvas-confetti dependencies.
  </action>
  <verify>Run npm run dev inside frontend directory and query http://localhost:3000</verify>
  <acceptance_criteria>
    - frontend/package.json exists and lists next, react, axios, tailwindcss
  </acceptance_criteria>
  <done>Next.js dashboard project template is initialized</done>
</task>

<task type="auto">
  <name>Task 2: API Integration and Axios Client</name>
  <files>frontend/src/services/api.ts</files>
  <read_first>frontend/package.json</read_first>
  <action>
    Create frontend/src/services/api.ts.
    Initialize default axios instance with baseUrl referencing backend API (e.g. `http://localhost:8000/api`).
    Add request interceptor to automatically read token from localStorage and append the Authorization Header (`Bearer token`).
    Add service endpoints: login, register, listProjects, createProject, uploadDataset.
  </action>
  <verify>Assert api.ts exports functions for auth, projects, and dataset upload</verify>
  <acceptance_criteria>
    - axios client handles JWT tokens and injects them in requests automatically
  </acceptance_criteria>
  <done>HTTP client is set up for communication with backend services</done>
</task>

<task type="auto">
  <name>Task 3: Global Authentication Context</name>
  <files>frontend/src/contexts/AuthContext.tsx,frontend/src/app/layout.tsx</files>
  <read_first>frontend/src/services/api.ts</read_first>
  <action>
    Create frontend/src/contexts/AuthContext.tsx.
    Define AuthContext and AuthProvider component.
    Manage user state, token state, register/login logic, and logout logic (clearing localStorage and states).
    Wrap root layout.tsx in the AuthProvider.
  </action>
  <verify>Confirm auth hook compiles and is retrievable by components</verify>
  <acceptance_criteria>
    - AuthProvider wraps children successfully
    - Hook useAuth() provides user, login, logout, and loading status
  </acceptance_criteria>
  <done>Session state manager hook is active</done>
</task>

<task type="auto">
  <name>Task 4: Premium Login and Registration Views</name>
  <files>frontend/src/app/login/page.tsx,frontend/src/app/register/page.tsx,frontend/src/app/page.tsx</files>
  <read_first>frontend/src/contexts/AuthContext.tsx</read_first>
  <action>
    Build register and login page forms in frontend/src/app/register/page.tsx and frontend/src/app/login/page.tsx.
    Integrate premium design assets: dark-mode glassmorphic cards, gradient borders, Outfit/Inter typography, responsive grids, and subtle button transition animations.
    Redirect root index page/dashboard routes based on active auth tokens.
  </action>
  <verify>Start frontend server and manually view login and registration card layout</verify>
  <acceptance_criteria>
    - Login and Register forms handle submit and display errors properly
    - Successful login saves JWT token to storage and redirects to /dashboard
  </acceptance_criteria>
  <done>Auth screen forms are ready and integrated with backend auth endpoints</done>
</task>

<task type="auto">
  <name>Task 5: Project Dashboard and Dataset Upload Views</name>
  <files>frontend/src/app/dashboard/page.tsx,frontend/src/components/ProjectCard.tsx,frontend/src/components/FileUploader.tsx</files>
  <read_first>frontend/src/services/api.ts</read_first>
  <action>
    Build main dashboard at frontend/src/app/dashboard/page.tsx.
    Create a project card component in frontend/src/components/ProjectCard.tsx displaying name, description, and list of uploaded datasets.
    Create a file upload box in frontend/src/components/FileUploader.tsx that supports drag-and-drop, validates file types (CSV/XLSX), calls the backend upload api, and displays the extracted rows/columns/missing values metadata upon success.
  </action>
  <verify>Manually open dashboard, click "Create Project", choose project, drag a test CSV file, and verify metadata fields populate automatically</verify>
  <acceptance_criteria>
    - Dashboard displays a list of projects fetched from backend
    - "Create Project" modal successfully saves new project to list
    - File uploader renders stats card after dataset is successfully parsed
  </acceptance_criteria>
  <done>Frontend project dashboard and file uploader are fully integrated with backend</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Build frontend: `npm run build` inside frontend directory.
- Verify user workflow from registration to dataset upload in browser.
</verification>

<success_criteria>
- Next.js app builds without compilation warnings/errors.
- Dashboard isolates datasets by active projects.
- UI elements display clean, responsive styles.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dataset-ingestion/01-03-SUMMARY.md`
</output>
